var x = 0;

fn callback_a(arg)
{
  io.println("callback_a", arg);
  
  if (!x) {
    x = other_func();
  }
  
  x();
  x();
  x();
}

fn callback_b(arg1, arg2)
{
  io.println("callback_b", arg1, arg2);
}

fn other_func() {
  var blah = 10;
  
  ret fn() {
    io.println("asdf" + @(blah++));
  };
}