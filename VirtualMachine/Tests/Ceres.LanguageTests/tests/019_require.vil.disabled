#include "lib/test.vil"

#[test]
fn included_function_invocation() {
  val result = included_add(10, 10) 
             + included_sub(5, 0)
             + included_mul(3, 3)
             + included_div(20, 4);
             
  assert.equal(result, 39);
}